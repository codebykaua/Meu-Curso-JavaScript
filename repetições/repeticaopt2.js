let numero = 5

// Começamos o multiplicador em 1 e vamos até o 10
for (let multiplicador = 1; multiplicador <= 10; multiplicador++) {
    
    // O JavaScript faz a conta de vezes (*) automaticamente dentro da chave ${ }
    console.log(`${numero} x ${multiplicador} = ${numero * multiplicador}`)
    
}